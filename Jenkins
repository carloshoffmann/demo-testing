pipeline {

    agent any
 
    environment {

        MOCHA_FILE = "./jenkins-test-results.xml"


    }


    stages {


        stage('Build') {

            steps {
                sh '''
                    echo "installing dependencies"
                    npm install
                '''
            }
        }


        stage('Test') {

            steps {
                
                archive MOCHA_FILE

                sh '''
                    echo "running tests"
                    ./node_modules/.bin/mocha test/** --reporter mocha-junit-reporter                   
                '''
                
            }
        }


    }

    post {

        always {

            mail to:"choffmann@ned.cl",
             subject:"jenkins script running!",
             body:"OK" 

             

        }

        failure {
            mail to:"choffmann@ned.cl",
             subject:"jenkins build fail!!!!",
             body:"OK" 
        }


        success {
            mail to:"choffmann@ned.cl",
             subject:"jenkins script running smoothly!",
             body:"OK" 

        }

        unstable {
            echo 'This will run only if the run was marked as unstable'
        }

        changed {
            echo 'This will run only if the state of the Pipeline has changed'
            echo 'For example, if the Pipeline was previously failing but is now successful'
        }
    


    }


}
